#include "main.h"

/**
 * _abs - a function that computes an absolute value of an integer
 * @b: the int to check
 * Return: the absolute value of int
 */
int _abs(int b)
{
	if (b >= 0)
	{
		return (b);
	}
	return (-b);
}
